# ✅ **Exercício 1 — Criar a Navbar e Configurar as Rotas**

### 🎯 Objetivo:

Aprender sobre roteamento, navegação e criação de componentes básicos.

### 🧩 Tarefas:

1. Criar o componente `navbar`:

   ```
   ng g c shared/navbar
   ```
2. Adicionar links para:

   * Dashboard
   * Sobre
   * Contato
3. Configurar `app-routing.module.ts` criando rotas:

   * `/dashboard`
   * `/sobre`
   * `/contato`
4. Inserir a navbar no `app.component.html`

### ✅ Entrega esperada:

* Navegação funcional entre as páginas
* Navbar fixa no topo
* Estrutura inicial de rotas funcionando

---

# ✅ **Exercício 2 — Criar o Layout Base do Dashboard**

### 🎯 Objetivo:

Construir a primeira tela do portal, trabalhar HTML e CSS.

### 🧩 Tarefas:

1. Criar a estrutura dos cards no `dashboard.component.html`
2. Criar um grid responsivo:

   ```css
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
   gap: 20px;
   ```
3. Criar 6 cards fictícios com:

   * ícone (pode ser imagem)
   * título
   * descrição

### ✅ Entrega esperada:

* Dashboard com cards bem organizados
* Layout responsivo (desktop/tablet/mobile)

---

# ✅ **Exercício 3 — Criar o Componente Reutilizável de Card**

### 🎯 Objetivo:

Introduzir componentização e `@Input()`.

### 🧩 Tarefas:

1. Criar componente:

   ```
   ng g c shared/card
   ```
2. Receber via `@Input()`:

   * `title: string`
   * `description: string`
   * `icon: string`
3. Atualizar o dashboard para usar o componente `<app-card>`.

### ✅ Entrega esperada:

* Cards reutilizáveis
* Dashboard limpo e organizado

---

# ✅ **Exercício 4 — Criar um Arquivo de Dados (Mock)**

### 🎯 Objetivo:

Ensinar estruturação de dados e organização.

### 🧩 Tarefas:

1. Criar `assets/data/cards.json` com conteúdo:

   ```json
   [
     { "title": "Card 1", "description": "Descrição 1", "icon": "icon1.png" },
     { "title": "Card 2", "description": "Descrição 2", "icon": "icon2.png" }
   ]
   ```
2. Criar um serviço:

   ```
   ng g s core/card
   ```
3. Ler o JSON com `HttpClient`.
4. Exibir os dados no dashboard.

### ✅ Entrega esperada:

* Dados externos carregando corretamente
* Dashboard populado automaticamente

---

# ✅ **Exercício 5 — Criar a Página "Sobre"**

### 🎯 Objetivo:

Trabalhar HTML semântico e escrita técnica.

### 🧩 Tarefas:

1. Criar texto institucional:

   * O que é o portal
   * Quem desenvolveu
   * Propósito do laboratório
2. Criar layout simples com:

   * título
   * parágrafos
   * imagem ilustrativa

### ✅ Entrega esperada:

Página visualmente organizada e responsiva.

---

# ✅ **Exercício 6 — Criar a Página “Contato”**

### 🎯 Objetivo:

Testar formulários com Angular.

### 🧩 Tarefas:

1. Criar formulário com:

   * nome
   * email
   * mensagem
2. Validar:

   * campo obrigatório
   * email válido
3. Exibir no console os dados enviados (não precisa back-end).

### ✅ Entrega esperada:

Formulário validado e funcional.

---

# ✅ **Exercício 7 — Responsividade Completa**

### 🎯 Objetivo:

Treinar design responsivo.

### 🧩 Tarefas:

1. Ajustar dashboard para:

   * 3 cards por linha (desktop)
   * 2 cards por linha (tablet)
   * 1 card por linha (mobile)
2. Ajustar navbar para colapsar em telas menores.

### ✅ Entrega esperada:

Site navegável e bonito em mobile.

---

# ✅ **Exercício 8 — Criar Estrutura de README para o Projeto**

### 🎯 Objetivo:

Ensinar boas práticas de documentação.

### 🧩 Tarefas:

1. Explicar:

   * propósito do projeto
   * como executar
   * onde estão as pastas
   * o que foi desenvolvido
2. Incluir screenshots

### ✅ Entrega esperada:

README profissional.

---

# ✅ **Exercício 9 — Criar um Serviço de Autenticação (Fake Login)**

### 🎯 Objetivo:

Introduzir conceitos reais de login, services, programação assíncrona, guards e fluxo de autenticação.

### 🧩 Tarefas:

1. Criar um formulário de login com email e senha.
2. Criar o serviço `auth.service.ts`:

   * Método `login()` que retorna uma Promise ou Observable simulando API (delay de 1–2s).

3. Salvar um token falso no `localStorage` ao logar.
4. Criar um **AuthGuard** que bloqueia acesso ao dashboard se não estiver logado.
5. Redirecionar para `/login` se tentar entrar sem permissão.

### 🎯 Entrega esperada:

Fluxo de login básico funcional, com rotas protegidas.

---

# ✅ **Exercício 10 — Criar Interceptor para Logs de Requisições**

### 🎯 Objetivo:

Aprender sobre interceptors, uma parte essencial de projetos Angular reais.

### 🧩 Tarefas:

1. Gerar interceptor:

   ```
   ng g interceptor core/http-logger
   ```

2. Logar no console:

   * URL da requisição
   * tempo de resposta
   * status

3. Mostrar `console.warn` quando um erro HTTP ocorrer.

### Resultado:

Mais consciência sobre ciclo de requisições HTTP.

---

# ✅ **Exercício 11 — Criar um CRUD Completo (Mockado)**

### 🎯 Objetivo:

Levar o aluno ao ponto mais próximo de um projeto real, com manipulação de dados.

### 🧩 Tarefas:

1. Criar uma página `/usuarios` listando usuários.
2. Criar componentes:

   * `usuarios-list`
   * `usuarios-form`
3. Criar serviço `usuarios.service.ts` usando JSON em `assets/users.json`.
4. Implementar:

   * listar
   * adicionar
   * editar
   * deletar
5. Usar **Input() e Output()** entre componentes.

### Resultado:

Fundamentos essenciais para qualquer aplicação real.

---

# ✅ **Exercício 12 — Modal Reutilizável**

### 🎯 Objetivo:

Aprender componentização mais profunda.

### 🧩 Tarefas:

1. Criar o componente:

   ```
   ng g c shared/modal
   ```
2. Permitir que receba:

   * título
   * conteúdo via `ng-content`
3. Usar o modal em:

   * confirmação de exclusão
   * detalhes de usuário

### Resultado:

Componentes reutilizáveis avançados.

---

# ✅ **Exercício 13 — Pagination e Search no CRUD**

### 🎯 Objetivo:

Treinar manipulação de arrays, lógica e UX.

### 🧩 Tarefas:

1. Criar um campo de pesquisa que filtra a lista.
2. Criar paginação manual:

   * 5 itens por página
3. Botões:

   * Próxima página
   * Página anterior

### Resultado:

Capacidade de resolver problemas reais de listagem.

---

# ✅ **Exercício 14 — Consumo de API Externa Real**

### 🎯 Objetivo:

Finalmente sair do mock e consumir um endpoint real.

### 🧩 Sugestão de API:

```
https://jsonplaceholder.typicode.com/users
```

### 🧩 Tarefas:

1. Criar serviço `api.service.ts`
2. Consumir dados reais e exibir numa página nova `/externo`
3. Tratar:

   * loading
   * erro
   * retry opcional

### Resultado:

Primeiro contato com API real.

---

# ✅ **Exercício 15 — Tema Dark/Light (com armazenamento local)**

### 🎯 Objetivo:

Ensinar CSS avançado + estado + localStorage.

### 🧩 Tarefas:

1. Criar toggle “Modo Escuro”.
2. Alterar variáveis CSS:

   ```css
   :root {
     --bg: #fff;
     --text: #000;
   }

   .dark {
     --bg: #121212;
     --text: #fff;
   }
   ```
3. Persistir a escolha no localStorage.

---

# 🚀 **Resultado Final: um mini-portal completo**
